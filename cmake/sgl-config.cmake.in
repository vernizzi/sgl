@PACKAGE_INIT@

# sgl has no external dependencies — the module sources compile against the
# standard library and the target's own (AVX2/FMA) usage requirements only.

include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@-targets.cmake")

check_required_components(@PROJECT_NAME@)
